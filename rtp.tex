\documentclass{article}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, arrows.meta, positioning, fit, backgrounds}

\title{\textbf{A Minimal MCU Gateway \\ of ANSI C99 with GStreamer \\
               for High-Density Video Conferencing \\
               on Erlang/OTP Control Plane \\
               in Alpine Linux under Kubernetes} \\ [1ex]
       {\LARGE Part of Zen Crypted X.422.2 Buddha Protocol}}
\author{Namdak Tonpa, Zen Crypted}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
This paper presents the architectural design, implementation, and systems capacity review of \texttt{rtp},
a Multipoint Control Unit (MCU) gateway designed for high-density, real-time video conferencing in isolated
distributed network namespaces. Traditional architectures (e.g., Selective Forwarding Units and
headless-browser-based recording egress systems) suffer from high CPU and memory overhead.
We propose a collapsed, monolithic Erlang/GStreamer node deployed as share-nothing Alpine
Linux pods. By binding all participants of a room to the same pod instance using Ingress
Sticky Sessions and disabling Erlang cluster distribution,
the system scales horizontally without inter-node cluster link storms. Real-time video grid
compositing (2x2 layout in 1080p) and audio mixing are delegated directly to a local,
lightweight C99 GStreamer port binary, exchanging WebRTC SDP/ICE signaling lines directly over
standard input and output pipes. We show that this model supports up to 10,000 concurrent signaling
connections and reduces media encoding resource requirements by over 60\% when hardware-accelerated.
\end{abstract}

\newpage
\tableofcontents

\newpage
\section{Introduction}

Real-time audio and video conferencing in distributed networks requires both high reliability (liveness) and low computational overhead (high room density per physical host). The critical importance of robust, low-latency communication infrastructure was demonstrated globally in March 2020, when pandemic lockdowns forced entire institutions online. Overnight, daily meeting participants on platforms like Zoom skyrocketed from 10 million to 300 million, Google Meet traffic crossed 100 million daily users, and Microsoft Teams usage grew by 894\% \cite{seanslabwebrtc}. Rather than a triumph of ad-hoc application engineering, this massive scale was made possible by the standardization of WebRTC (Web Real-Time Communication), which enabled browsers to capture, encrypt, and stream real-time audio and video peer-to-peer with sub-second latency.

\subsection{Historical Precursors and GStreamer Integration}

The origin of WebRTC dates back to Stockholm in 1999 with the founding of Global IP Solutions (GIPS) by signal processing researchers Roar Hagen, Bastiaan Kleijn, Espen Fjogstad, and Ivar T. Hognestad \cite{seanslabwebrtc}. GIPS identified that VoIP quality over packet-switched networks was primarily a signal processing problem rather than a networking issue, combatting packet loss, jitter, clock drift, and acoustic echo using adaptive algorithms. They developed legendary narrowband and wideband codecs like iLBC (RFC 3951) and iSAC, along with advanced media engines for acoustic echo cancellation (AEC), noise suppression, and automatic gain control (AGC). Licensing their software to Yahoo!, AOL, Cisco WebEx, and Google Talk, GIPS became the industry reference stack.

In May 2010, Google acquired GIPS for \$68.2 million, and subsequently open-sourced the entire engine on May 31, 2011, under the leadership of Harald Alvestrand and Justin Uberti \cite{seanslabwebrtc}. This open-source release formed the technical foundation for the dual-track standardization process: the W3C WebRTC Working Group (defining the client JavaScript API) and the IETF RTCWeb Working Group (defining the underlying protocols). The subsequent decade saw intense engineering battles, including the Mandatory To Implement (MTI) video codec war between Google's royalty-free VP8 codec and the telecom industry's H.264 standard, culminating in the 2014 compromise to support both codecs. This was followed by the Session Description Protocol (SDP) negotiation debates, leading to Microsoft's brief ORTC (Object RTC) implementation in Edge and the eventual hybrid APIs adopted in WebRTC 1.0. Crucially, to enable WebRTC capabilities outside of traditional browser engines, early implementations like Ericsson Research's OpenWebRTC project \cite{openwebrtc} pioneered the use of the GStreamer multimedia framework to process media tracks natively on mobile devices and servers, directly prefiguring standalone native MCU systems.

\subsection{Conferencing Topologies: P2P, SFU, and MCU}

Modern real-time communication architectures leverage three primary media distribution topologies:

\begin{enumerate}
    \item \textbf{Direct Peer-to-Peer (P2P)}: In 1-on-1 configurations ($K=2$), participants stream media directly between endpoints (or via STUN/TURN relay) bypassing the media server entirely. Our orchestrator detects this case dynamically, configuring direct signaling channels. This simplified P2P connection minimizes server resource usage to near zero, providing a highly efficient baseline fallback.
    \item \textbf{Selective Forwarding Unit (SFU)}: In multi-party conferences ($K \ge 3$), SFUs route separate audio and video packets from each publisher to all other participants without transcoding. While this saves server CPU cycles, it shifts the computational and bandwidth burden to endpoints, which must decode $K-1$ incoming streams and handle network fluctuations for each. In resource-constrained enterprise networks, mobile clients, and high-security endpoints, this multi-decoder load is a major failure point. Thus, the SFU model can be completely abandoned for this architecture.
    \item \textbf{Multipoint Control Unit (MCU)}: To ensure $O(1)$ client bandwidth and decoding complexity, we enforce a centralized MCU model. The server decodes all incoming feeds, mixes audio tracks, composites the video streams into a single grid canvas, and encodes a unified broadcast feed back to all endpoints. By running this pipeline in C99 using GStreamer and sharding room instances horizontally across stateless Alpine Linux pods, we bypass the traditional CPU bottlenecks of monolithic MCUs, making the SFU completely obsolete for high-density, secure deployments.
\end{enumerate}

\newpage
\section{System Architecture}

The system consists of three distinct layers: the proxy/routing ingress, the Erlang orchestrator,
and the GStreamer media compositor.

\subsection{Architectural Topology}
Figure~\ref{fig:topology} shows the physical and logical entities, protocols, and network connections.

\begin{figure}[htbp!]
\centering
\begin{tikzpicture}
[
    % ── Node styles ──────────────────────────────────────────────────────────────
    block/.style={draw=blue!70, fill=blue!5, rectangle, minimum height=2.8em, minimum width=6.8em, rounded corners=4pt, align=center, font=\small, line width=0.8pt},
    iblock/.style={draw=violet!70, fill=violet!5, rectangle, minimum height=2.8em, minimum width=6.8em, rounded corners=4pt, align=center, font=\small, line width=0.8pt},
    tblock/.style={draw=teal!70, fill=teal!5, rectangle, minimum height=2.8em, minimum width=6.8em, rounded corners=4pt, align=center, font=\small, line width=0.8pt},
    client/.style={draw=orange!80, fill=orange!5, rectangle, minimum height=4.5em, minimum width=9.5em, rounded corners=6pt, align=center, font=\small, thick, line width=1.2pt},
    % ── Arrow styles ─────────────────────────────────────────────────────────────
    arrow/.style={-{Stealth[scale=1.0]},  thick, draw=blue!70},
    iarrow/.style={-{Stealth[scale=1.0]}, thick, draw=violet!70},
    tarrow/.style={-{Stealth[scale=0.9]}, thick, draw=teal!70, dashed}
]

    % ═══════════════════════════════════════════════════════════════════════════════
    % GRID:  x_left=1.5  x_centre=3.5  x_right=5.5  |  Δy=1.5  |  ns_pad=0.6
    %
    % erp-infra  boundary: (1.7, 2.1) – (5.3, 4.3)   [centre Traefik at (3.5, 3.2)]
    % erp-rtp    boundary: (-0.3, -2.6) – (7.3, 1.1)  [gap 1.0 cm below erp-infra]
    % erp-telemetry boundary: (-0.3, -8.8) – (7.3,-3.6) [gap 1.0 cm below erp-rtp]
    % ═══════════════════════════════════════════════════════════════════════════════

    % ── erp-infra namespace ───────────────────────────────────────────────────────
    \node[iblock] (ingress) at (0.68, 3.0) {Traefik Ingress \\ (Sticky Session)};
    \draw[dashed, violet!50, line width=1pt, rounded corners=6pt] (-2.02, 2.1) rectangle (8.29, 4.24);
    \node[anchor=north east, font=\footnotesize\bfseries\color{violet!70}] at (5.01, 4.12) {erp-infra};

    % ── erp-rtp namespace ─────────────────────────────────────────────────────────
    % Left col: GStreamer (y=0.0) / eturnal (y=−1.5)
    % Right col: N2O (y=0.0)      / Coord   (y=−1.5)

    \node[block] (gst)     at (0.58,  0.37) {C99 GStreamer \\ (gst)};
    \node[block] (eturnal) at (1.59, -1.16) {eturnal TURN \\ (Port 3478)};
    \node[block] (n2o)     at (6.02, -0.07) {N2O Gateway \\ (Port 8081)};
    \node[block] (coord)   at (4.89, -1.9)  {Room Coordinator \\ (GenServer)};
    \draw[dashed, gray!50, line width=1pt, rounded corners=6pt] (-2.02, -2.6) rectangle (8.31, 1.1);
    \node[anchor=north east, font=\footnotesize\bfseries\color{gray!70}] (node2) at (4.79, 0.92) {erp-rtp};

    % ── Client – outside all namespaces (vertically centred on erp-rtp left col) ─
    \node[client] (browser) at (-5.5, -0.75) {\textbf{SPA PWA} \\ (N2O.JS)};

    % ── erp-infra → erp-rtp: Traefik feeds N2O ───────────────────────────────────
    \draw[iarrow, ->] (ingress.east) to[out=0, in=90] node[right=10pt, font=\tiny, pos=0.5]{} (n2o.north);
    \draw[iarrow, ->] (n2o) to[out=269, in=90] (coord.north);

    % ── Client → namespaces: 3-anchor fan ────────────────────────────────────────
    % browser.north → Traefik  (steep upward arc, WSS/HTTPS)
    \draw[arrow, <->] (browser.north) to[out=90, in=180] node[left=10pt, font=\tiny, pos=0.5] {WSS / HTTPS} (ingress.west);

    % browser.north east → GStreamer  (gentle upward, WebRTC SRTP)
    \draw[arrow, <->] (browser.north east) to[out=60, in=141] node[above=7pt, font=\tiny, very near start, pos=0.5] {WebRTC SRTP} (gst.west);
    \draw[arrow, <->] (coord.north west) to[out=89, in=3] node[right=10pt, font=\tiny, pos=0.8] {WebRTC SRTP} (gst.east);

    % browser.south east → eturnal  (gentle downward, STUN/TURN)
    \draw[arrow, <->] (browser.south east) to[out=297, in=267] node[below=7pt, font=\tiny, pos=0.5] {STUN/TURN} (eturnal.south west);

    % ═══════════════════════════════════════════════════════════════════════════════
    % erp-telemetry namespace
    % Row 1: rtp-telemetry (3.5, -4.7)  — OTLP aggregator
    % Row 2: otel-collector (3.5, -6.2)
    % Row 3: grafana (1.5), prometheus (3.5), opensearch (5.5)  at y=−7.7
    % ═══════════════════════════════════════════════════════════════════════════════
    \node[tblock] (rtptel)   at (3.5, -4.7) {rtp-telemetry \\ (OTLP Receiver)};
    \node[tblock] (otel)     at (3.5, -6.2) {otel-collector \\ (Port 4317)};
    \node[tblock] (grafana)  at (-0.01, -7.38) {Grafana \\ (Dashboards)};
    \node[tblock] (prom)     at (3.51, -8.24) {Prometheus \\ (Metrics)};
    \node[tblock] (opensrch) at (6.5, -7.37) {OpenSearch \\ (Logs)};
    \draw[dashed, teal!50, line width=1pt, rounded corners=6pt] (-2.02, -9.0) rectangle (8.3, -3.6);
    \node[anchor=north east, font=\footnotesize\bfseries\color{teal!70}] at (4.8, -3.76) {erp-telemetry};

    % ── Telemetry fan: all 4 erp-rtp pods → rtp-telemetry (out=−90, varying in) ─
    \node[font=\tiny, text=teal!70] (node1) at (3.5, -3.1) {Telemetry (OTLP)};

    \draw[tarrow, ->] (n2o.east) to[out=291, in=0] (rtptel.east);
    \draw[tarrow, ->] (eturnal.south) to[out=-90, in=140]  (rtptel.north west);
    \draw[tarrow, ->] (gst.south west) to[out=199, in=148] (rtptel.west);
    \draw[tarrow, ->] (browser.south) to[out=270, in=180] (rtptel.south west);
    \draw[tarrow, ->] (coord.south) to[out=219, in=27] (rtptel.north east);

    % ── Internal erp-telemetry pipeline ──────────────────────────────────────────
    \draw[tarrow, ->] (rtptel.south) -- (otel.north);
    \draw[tarrow, ->] (otel.south) to[out=-90, in=90] (grafana.north);
    \draw[tarrow, ->] (otel.south)  --                (prom.north);
    \draw[tarrow, ->] (otel.south) to[out=-90, in=90] (6.86,-7);

\end{tikzpicture}
\caption{Three-namespaces architecture.}
\label{fig:topology}
\end{figure}

\subsection{Ingress Sticky Routing \& Room Sharding}

To scale the horizontal architecture to 1,000+ concurrent rooms, we bypass Erlang distribution.
The Ingress proxy (Nginx or Envoy) parses the Room ID from the connection URI path (e.g.,
\texttt{/room/\{room\_id\}/signaling}) and maps it to a consistent target pod IP using a hash ring:

\begin{equation}
\text{Pod IP} = \text{Hash}(\text{room\_id}) \pmod P
\end{equation}

Where $P$ is the total count of running replicas. This ensures that:

\begin{itemize}
    \item All participants of a specific room land on the same pod.
    \item The room signaling pub/sub is fully handled in-memory locally using N2O.
    \item The Erlang nodes run with no distribution, avoiding the $O(P^2)$ storm.
    \item Mnesia acts as a purely local persistent transaction ledger mounted on an Alpine Linux local directory PVC.
\end{itemize}

\subsection{WebRTC Signaling Loop \& C99 Port Protocol}

The C99 GStreamer binary (\texttt{gst}) is spawned as an OS process by the Erlang supervisor.
Rather than linking against heavy socket libraries, it reads signaling data from \texttt{stdin} and outputs event payloads to \texttt{stdout}.

\subsubsection{Protocol Stack and Specifications}
The monolith interfaces utilize a diverse set of network and inter-process communication (IPC) protocols
to coordinate signaling, manage media routing, and report client telemetry.
Table~\ref{tab:protocols} provides a comprehensive description of each protocol,
its transport layer, and operational parameters.

\begin{table}[h]
\centering
\caption{System Protocols and Inter-Process Specifications}
\label{tab:protocols}
\noindent\makebox[\textwidth][c]{
\begin{tabular}{lp{1.5cm}llp{6.5cm}}
\toprule
Protocol                & Port       & STD      & Format    & Functional Description \\
\midrule
\textbf{GST IPC}        & UNIX Pipes & own      & Format    & Custom Erlang-to-C99 binary signaling exchange for SDP offers/answers and ICE candidates. \\
\textbf{WS Signaling}   & WSS:8081   & own      & WSS       & Cowboy/N2O room pub/sub channel for user joins, leaves, and chat logs. \\
\textbf{QoS Telemetry}  & WSS:8082   & own      & WSS       & Prioritized client connection statistics pumped to OpenTelemetry collector. \\
\textbf{WebRTC SDP}     & UDP:49232  & RFC 8841 & SDP       & SCTP over DTLS. \\
\textbf{WebRTC SDP}     & UDP:49232  & RFC 8866 & SDP       & Session negotiation declaring codecs (H.264/VP8/Opus) and media directions. \\
\textbf{WebRTC ICE}     & UDP:3478   & RFC 8445 & STUN/TURN & RTP STUN Binding Attributes Dynamic network connectivity probe establishing optimal media pathways. \\
\textbf{TURN Relay}     & UDP:5349   & RFC 8489 & STUN/TURN & SRTP media packets through \texttt{eturnal} when NAT/firewalls block P2P. \\
\textbf{SRTP/SRTCP}     & UDP:5000   & RFC 3711 & SRTP/RTCP & Transports secure real-time audio and video tracks between endpoints and GStreamer. \\
\bottomrule
\end{tabular}
}
\end{table}

\subsubsection{JSON Replacement Strategy on Existing Connections}
\label{sec:json-replacement}

The current implementation uses JSON for several high-frequency control and signaling paths.
We propose replacing these with structured binary formats based on relevant ITU-T X-series
Recommendations (from \texttt{synrc.com/standards.txt}), particularly the multi-peer, group, and managed P2P frameworks.

\begin{table}[h]
\centering
\caption{JSON Replacement Using ITU-T X-Series Standards}
\label{tab:json-replacement}
\noindent\makebox[\textwidth][c]{
\begin{tabular}{lll}
\toprule
Port & Current Format & Applicable ITU-T X-Recommendation(s) \\
\midrule
8081 & JSON (N2O)  & X.601 (Multi-peer framework), X.602 (Group management), X.609.x (Managed P2P) \\
Unix & Ad-hoc JSON & X.603 / X.603.1 / X.603.2 (Relayed multicast), X.609.3 / X.609.4 (streaming signalling/peer) \\
8082 & JSON/OTLP   & X.609.8 / X.609.10 (Live data sources \& data streaming signaling) \\
Room & JSON events & X.601 / X.602 (Multi-peer \& group management) \\
\bottomrule
\end{tabular}
}
\end{table}

\paragraph{Selected ITU-T X-Stack Standards}
The following recommendations from the X-series provide a formal framework for group video conferencing:
\begin{itemize}
    \item \textbf{X.601} — Multi-peer communications framework
    \item \textbf{X.602} — Group management protocol
    \item \textbf{X.603 / X.603.1 / X.603.2} — Relayed multicast protocol (simplex / N-plex)
    \item \textbf{X.604 / X.604.1 / X.604.2} — Mobile multicast communications
    \item \textbf{X.605 -- X.608.1} — Enhanced Communications Transport Service (ECTS) for multicast
    \item \textbf{X.609 -- X.609.10} — Managed peer-to-peer (P2P) communications (especially X.609.3 / X.609.4 for multimedia streaming signaling and peer protocols)
\end{itemize}

\paragraph{Implementation Approach in \texttt{zencrypted/rtp}}
\begin{enumerate}
    \item Define signaling and control PDUs using ASN.1 (X.680) modules that align with the semantics of X.601 / X.609.x.
    \item Use N2O's ASN.1 formatter to replace JSON on WebSocket connections (Port 8081).
    \item Extend the C99 GStreamer port (\texttt{gst.c}) to use binary messages (inspired by X.603 relayed multicast and X.609.4 peer protocols) over stdin/stdout.
    \item For telemetry and room events, adopt structures from X.609.8 / X.609.10.
    \item Retain RTP/SRTP for the media plane (already ITU-aligned via RFC 3550).
\end{enumerate}

 Expected outcomes: significantly reduced signaling overhead, formal compliance with ITU-T multi-peer frameworks, and improved scalability for high-density rooms.

\subsubsection{Detailed Protocol Descriptions}

\paragraph{1. WebRTC SDP \& ICE (RTP/RTCP Data)}
The Session Description Protocol (SDP) is exchanged in plain text (RFC 8866) over the WebSocket signaling channel. It acts as the negotiation mechanism for media capabilities, where the GStreamer OS process outputs SDP offers containing its supported codecs (e.g., H.264, VP8, Opus) and payload types. The client browser responds with an SDP answer. Simultaneously, Interactive Connectivity Establishment (ICE) candidates are exchanged to discover the optimal network path. Once the ICE handshake completes, the actual media data is transmitted as encrypted SRTP (Secure Real-time Transport Protocol) and SRTCP packets over UDP.

\paragraph{2. TURN (Traversal Using Relays around NAT)}
When direct Peer-to-Peer (P2P) UDP connections are blocked by restrictive enterprise firewalls or symmetric NATs, the system falls back to the \texttt{eturnal} TURN server. The TURN protocol (RFC 8656) encapsulates the SRTP media packets within its own framed binary format and relays them through port 3478 (UDP or TCP). This guarantees 100\% connectivity for all participants, albeit with a slight latency and bandwidth penalty due to the relay overhead.

\paragraph{3. WebSocket API}
The WebSocket (WS) API serves as the primary signaling and control plane, powered by the Cowboy web server and the N2O framework. It operates over WSS (Port 8081) using a binary-encoded or JSON text payload. The WS connection is stateful and sticky-routed to the specific Erlang node hosting the room. It handles pub/sub events such as \texttt{peer\_joined}, \texttt{peer\_left}, chat messages, and the critical SDP/ICE signaling payloads.

\paragraph{4. Telemetry (OTLP)}
The system employs the OpenTelemetry Protocol (OTLP) to export QoS metrics, traces, and logs. Client browsers and the backend Erlang nodes stream telemetry data (such as packet loss, jitter, and CPU utilization) to the \texttt{rtp-telemetry} OTLP Receiver via HTTPS/WSS (Port 8082). The payloads are serialized as Protocol Buffers (Protobuf) or JSON. This data is then ingested by the \texttt{otel-collector} and routed to Prometheus and OpenSearch for real-time observability in Grafana.

\subsubsection{Signaling Sequence Flow}

Figure~\ref{fig:sequence} illustrates the signaling loop during room startup, WebRTC negotiation, media composition, and session finalization.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    scale=0.85, every node/.style={transform shape},
    line/.style={draw, thick, dashed},
    arrow/.style={draw, thick, ->, >=stealth}
]
    % Lifelines
    \draw[line] (0,0) -- (0,-10) node[below] {Browser Client};
    \draw[line] (4,0) -- (4,-10) node[below] {Cowboy / N2O};
    \draw[line] (8,0) -- (8,-10) node[below] {media\_broker\_srv};
    \draw[line] (12,0) -- (12,-10) node[below] {gst (C99)};

    % Headings
    \node[above] at (0,0) {\textbf{Browser Client}};
    \node[above] at (4,0) {\textbf{Cowboy / N2O}};
    \node[above] at (8,0) {\textbf{media\_broker\_srv}};
    \node[above] at (12,0) {\textbf{gst}};

    % Step 1
    \draw[arrow] (0,-1) -- node[above, scale=0.8] {WS Connect (room\_1)} (4,-1);
    \draw[arrow] (4,-1.5) -- node[above, scale=0.8] {start\_mixing(room\_1)} (8,-1.5);
    \draw[arrow] (8,-2) -- node[above, scale=0.8] {open\_port("gst /tmp/r1.mp4")} (12,-2);

    % Step 2
    \node[draw, fill=yellow!10, text width=6cm, align=center, scale=0.8] at (6,-3) {GStreamer Init \& Base Pipeline Play};

    % Step 3
    \draw[arrow] (4,-4) -- node[above, scale=0.8] {\{peer\_joined, "peer\_A"\}} (8,-4);
    \draw[arrow] (8,-4.5) -- node[above, scale=0.8] {stdin: \{"type":"peer\_joined","peer\_id":"peer\_A"\}} (12,-4.5);

    % Step 4
    \draw[arrow] (12,-5.5) -- node[above, scale=0.8] {stdout: \{"type":"sdp\_offer","sdp":"..."\}} (8,-5.5);
    \draw[arrow] (8,-6) -- node[above, scale=0.8] {Forward Offer} (4,-6);
    \draw[arrow] (4,-6.5) -- node[above, scale=0.8] {WS: SDP Offer} (0,-6.5);

    % Step 5
    \draw[arrow] (0,-7.5) -- node[above, scale=0.8] {WS: SDP Answer} (4,-7.5);
    \draw[arrow] (4,-8) -- node[above, scale=0.8] {stdin: \{"type":"sdp\_answer","sdp":"..."\}} (12,-8);

    % Step 6
    \node[draw, fill=green!10, text width=8cm, align=center, scale=0.8] at (6,-9) {RTP Audio/Video Stream active. Composite Mixer rendering to file.};

\end{tikzpicture}
\caption{Signaling sequence showing local Port I/O communication between Erlang and the C99 GStreamer binary.}
\label{fig:sequence}
\end{figure}

\newpage
\subsection{Multipoint Media Composition}

The GStreamer media broker executes video grid compositing and audio conferencing concurrently. The media plane runs as a single GStreamer pipeline initialized in a C99 OS port process.

\subsubsection{Video Matrix Grid Compositing}

Incoming H.264/VP8 video packets are decoded into raw YUV frames. The compositor element (\texttt{compositor}) maps each input stream index $i \in \{1, 2, 3\}$ to a quadrant layout inside a unified 1080p canvas ($W_{\text{out}} = 1920$, $H_{\text{out}} = 1080$). The dimensions of each video frame are scaled to $w = 960$ and $h = 540$. The placement coordinates $(x_i, y_i)$ are formulated as:

\begin{align}
x_i &= (i \pmod 2) \cdot w \\
y_i &= \lfloor i / 2 \rfloor \cdot h
\end{align}

The resulting video layout is composed as shown in Equation~\ref{eq:layout}:
\begin{equation}\label{eq:layout}
\text{Canvas}[x, y] = 
\begin{cases} 
\text{Stream}_0[x, y] & 0 \le x < 960,\ 0 \le y < 540 \\
\text{Stream}_1[x - 960, y] & 960 \le x < 1920,\ 0 \le y < 540 \\
\text{Stream}_2[x, y - 540] & 0 \le x < 960,\ 540 \le y < 1080 \\
\text{Stream}_3[x - 960, y - 540] & 960 \le x < 1920,\ 540 \le y < 1080
\end{cases}
\end{equation}

\subsubsection{Audio Mixing \& Summation}

Audio tracks are mixed using GStreamer's \texttt{audiomixer} element. Incoming Opus packets are decoded into linear pulse-code modulated (LPCM) audio buffers. The mixed audio signal $S_{\text{mixed}}(t)$ at timestamp $t$ is the sum of the $K$ active participant signals:

\begin{equation}
S_{\text{mixed}}(t) = \sum_{i=1}^{K} S_i(t)
\end{equation}

To prevent digital clipping/overflow, the mixed LPCM buffer passes through a limiter/clipper stage before being re-encoded into Opus for WebRTC downstream broadcast:

\begin{equation}
S_{\text{output}}(t) = \text{Clamp}(S_{\text{mixed}}(t), -S_{\text{max}}, S_{\text{max}})
\end{equation}

\newpage
\subsubsection{C99 GStreamer Pipeline Implementation}

The implementation in \texttt{c\_src/gst.c} integrates WebRTC media reception, grid composition, and recording:

\begin{enumerate}
    \item \textbf{Continuous Live Sources}: To prevent preroll deadlocks when no peers are connected, the pipeline initializes with a continuous static background black video (\texttt{videotestsrc pattern=black}) and audio silence (\texttt{audiotestsrc volume=0}) bound permanently to pad \texttt{sink\_0} on the compositor and audiomixer.
    \item \textbf{Explicit Layering (Z-Ordering)}: GStreamer's \texttt{compositor} layers input pads according to the \texttt{zorder} property. The background pad is configured with $\text{zorder} = 1$. When a dynamic peer video track is decoded in the \texttt{on\_decoded\_pad} callback, the calculated compositor pad is assigned an explicit, higher $\text{zorder} = \text{idx} + 10$ (e.g., $11, 12, 13$), layering it on top of the black canvas.
    \item \textbf{Loop Decoupling and Leaky Queues}: Because each peer's \texttt{webrtcbin} acts as both a media source and a broadcast destination, the graph contains feedback loops. We introduce independent \texttt{tee} elements (\texttt{vtee}/\texttt{atee}) to broadcast the mixed grid back to all clients. Each broadcast branch is isolated via dynamic queues (\texttt{vqueue\_<peer\_id>}) configured with leaky downstream properties (\texttt{leaky=2}) and restricted capacity (\texttt{max-size-buffers=60}), preventing slow or disconnected peers from blocking the upstream compositor.
    \item \textbf{Dynamic Peer Lifecycle Management}: We track peer resources in a structured \texttt{PeerInfo} context. On receiving a \texttt{"peer\_left"} signal via \texttt{stdin}, GStreamer invokes \texttt{cleanup\_peer()}, which systematically unlinks pads, releases requested compositor/mixer pads, destroys the peer's converter and queue elements, and frees its \texttt{webrtcbin}. This prevents grid placement leakage and ensures memory and CPU utilization scale with the active stream count rather than historical connection counts.
    \item \textbf{Safe Unix Signal finalization}: Direct Unix signals run outside the main loop thread and are unsafe to manipulate GStreamer bins directly. We use GLib's thread-safe \texttt{g\_unix\_signal\_add} to schedule signal handling on the main event thread. When a termination signal is received, the handler sends an \texttt{EOS} event specifically to the recording muxer (\texttt{mp4mux}), which flushes standard MP4 index trailers safely before exit.
\end{enumerate}

\newpage
\section{Capacity, Telemetry and Metrics}

This section evaluates resource provisioning and cluster capacity for a target workload of \textbf{1,000 concurrent MCU rooms} (compositing multiple 720p streams @ 30~fps into unified outputs) with hardware acceleration. The target distribution comprises:
\begin{itemize}
    \item \textbf{80\% (800 rooms)}: 6-participant rooms.
    \item \textbf{20\% (200 rooms)}: 20-participant rooms.
\end{itemize}

To satisfy these requirements while minimizing costs, we specify a heterogeneous GPU-accelerated cluster consisting of \textbf{40 physical nodes}: 20 Small Nodes and 20 Big Nodes.

\subsection{Node Specifications}

Table~\ref{tab:node_specs} outlines the hardware profiles for the Small and Big nodes configured with GPU acceleration and cost-effective DDR4 memory.

\begin{table}[htbp]
\centering
\caption{Node Types and Specifications (GPU-Accelerated, Lower DDR4)}
\label{tab:node_specs}
\noindent\makebox[\textwidth][c]{
\begin{tabular}{lcc}
\toprule
\textbf{Component} & \textbf{Small Node (6-person)} & \textbf{Big Node (20-person)} \\
\midrule
Target Use & 6-participant rooms & 20-participant rooms \\
CPU & Single Xeon Silver/Gold (16--24 cores) & Dual Xeon Gold/Platinum (32--64+ cores) \\
RAM & 64--96\,GB DDR4 & 128\,GB DDR4 \\
GPU & 1 $\times$ Mid-High NVIDIA (e.g., RTX 4090 / A10) & 2 $\times$ High-End NVIDIA (e.g., A40 / L40S) \\
Network & 25--40\,Gbps & 40--100\,Gbps \\
Storage & 256\,GB NVMe SSD & 512\,GB NVMe SSD \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{Per-Node and Cluster Capacity}

Under GPU acceleration, decoding is performed via NVDEC and encoding via NVENC, drastically reducing CPU core utilisation. Table~\ref{tab:node_capacity} lists the real-time room capacity per node type, and Table~\ref{tab:cluster_capacity} summarizes the total cluster capacity and resource footprint.

\begin{table}[htbp]
\centering
\caption{Real-Time Capacity per Node}
\label{tab:node_capacity}
\noindent\makebox[\textwidth][c]{
\begin{tabular}{lcc}
\toprule
\textbf{Metric} & \textbf{Small Node} & \textbf{Big Node} \\
\midrule
Rooms per Node & 45 & 12 \\
Input Streams per Node & $\approx$ 270 & $\approx$ 240 \\
Output Streams per Node & 45 & 12 \\
\bottomrule
\end{tabular}
}
\end{table}

\begin{table}[htbp]
\centering
\caption{Total Cluster Capacity and Resource Summary}
\label{tab:cluster_capacity}
\noindent\makebox[\textwidth][c]{
\begin{tabular}{lrrr}
\toprule
\textbf{Category} & \textbf{Small Nodes (20)} & \textbf{Big Nodes (20)} & \textbf{Total / Headroom} \\
\midrule
6-Person Rooms & 900 & -- & 900 (\textbf{+100 headroom}) \\
20-Person Rooms & -- & 240 & 240 (\textbf{+40 headroom}) \\
Max Mixed Rooms & -- & -- & \textbf{1,140} \\
Target Workload & 800 & 200 & \textbf{1,000 (covered with 14\% headroom)} \\
\midrule
Total DDR4 RAM & & & $\approx$ 3.8--4.2\,TB \\
Total NVIDIA GPUs & & & 60 \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{System Model \& Parameter Definitions}

We model the memory and CPU consumption of the GStreamer MCU processes to validate these provisioning limits.

\subsubsection{Memory Allocation Model}
The total RAM consumption $M_{\text{total}}$ of a node is formulated as:
\begin{equation}
M_{\text{total}} = M_{\text{base}} + N \cdot M_{\text{conn}} + R \cdot (M_{\text{room}} + M_{\text{gst}}(K))
\end{equation}
Where the GStreamer media compositor RAM footprint for $K$ active peer connections is:
\begin{equation}
M_{\text{gst}}(K) = M_{\text{base\_gst}} + K \cdot M_{\text{peer\_gst}}
\end{equation}

For a Small Node hosting $R = 45$ rooms with $K = 6$ participants each:
\begin{itemize}
    \item $M_{\text{base\_gst}} \approx 120$~MB (base GStreamer and pipeline structures).
    \item $M_{\text{peer\_gst}} \approx 35$~MB (buffers and decode pipelines per 720p stream).
    \item $M_{\text{gst}}(6) = 120 + 6 \cdot 35 = 330$~MB per room.
    \item $M_{\text{total}} \approx 45 \cdot (330 + 0.005) \approx 14.85$~GB of heap/process memory, easily fitting within the 64--96\,GB DDR4 Small Node configuration.
\end{itemize}

Similarly, for a Big Node hosting $R = 12$ rooms with $K = 20$ participants each:
\begin{itemize}
    \item $M_{\text{gst}}(20) = 120 + 20 \cdot 35 = 820$~MB per room.
    \item $M_{\text{total}} \approx 12 \cdot (820 + 0.005) \approx 9.84$~GB process RAM, fitting well within the 128\,GB DDR4 specification. The remaining host memory provides ample caching, OS buffers, and network ring buffers.
\end{itemize}

\subsubsection{CPU and GPU Offloading Model}

With GPU-offloaded media pipelines, the total CPU load $C_{\text{total}}$ remains low because the heavy matrix composition, pixel format conversion, and H.264 encoding are executed on CUDA and NVENC. The node's processing capability is instead bound by the GPU's hardware session limits (NVENC concurrent streams) and network throughput.

The network throughput $T_{\text{net}}$ per node is defined by incoming and outgoing media streams:
\begin{equation}
T_{\text{net}} = R \cdot \left( K \cdot B_{\text{in}} + B_{\text{out}} \right)
\end{equation}
Where $B_{\text{in}}$ is the incoming client bitrate (1.5~Mbps H.264 720p) and $B_{\text{out}}$ is the mixed output canvas bitrate (4.0~Mbps 1080p).
\begin{itemize}
    \item \textbf{Small Node Traffic}: $45 \cdot (6 \cdot 1.5 + 4.0) = 585$~Mbps. This is well within the 25--40\,Gbps network interface capacity.
    \item \textbf{Big Node Traffic}: $12 \cdot (20 \cdot 1.5 + 4.0) = 408$~Mbps. This is comfortably supported by the 40--100\,Gbps interfaces, ensuring no network queuing jitter.
\end{itemize}

\subsection{Systems Soundness \& Liveness Analysis}

\subsubsection*{HPA Oscillation (Thundering Herd)}

Configuring Kubernetes Horizontal Pod Autoscaling (HPA) against standard CPU limits (e.g., 80\% target) creates scaling instabilities when CPU is offloaded to GPUs. Because CPU utilization remains low under hardware acceleration, standard HPA fails to react to room saturation. The autoscaling algorithm must instead utilize custom metrics mapped directly to active GStreamer mixers (\texttt{active\_gst\_mixers}) and GPU memory utilization.

\subsubsection*{Local Database Isolation (No Database Clustering)}

Under our sharded node model, Mnesia runs strictly as a local persistent storage database inside each pod's local volume. Because Erlang nodes do not form a distribution cluster, database transaction locking and schema updates are restricted to the local host. This completely avoids split-brain partitions, node discovery synchronization issues, and cluster-wide database deadlocks during rapid scale-up/down events.

\subsubsection*{Scale-Out Architecture Bottlenecks at 1000+ Rooms}

When scaling the cluster horizontally to support $R = 1,000$ active rooms across the 40 GPU-accelerated nodes:
\begin{enumerate}
    \item \textbf{Erlang Distribution Overhead Bypassed}: By routing all room participants to the same node via Ingress Sticky Session hashing, signaling is fully handled locally in-memory. Consequently, the Erlang distribution mechanism is completely disabled (the nodes run with \texttt{-no\_distribution}), totally avoiding quadratic $O(P^2)$ node connection storms.
    \item \textbf{Mnesia Transaction Locking Isolated}: Since Mnesia runs as a purely local database on each isolated host, transaction lock contention is strictly confined to the local node's processes, eliminating distributed two-phase commit latency.
    \item \textbf{Network Port \& Bandwidth Saturation}: At an average stream bitrate of $1.5$~Mbps, the aggregate network ingestion throughput reaches $4,000 \times 1.5\text{ Mbps} = 6.0$~Gbps. SREs must deploy network interfaces using \texttt{hostNetwork} configurations with SRIOV-enabled interfaces to bypass container network encapsulation.
\end{enumerate}

\section{Conclusion}

The proposed monolithic architecture is highly sound for large-scale signaling and STUN/TURN routing, easily supporting over 10,000 concurrent user events. By transitioning to hardware-accelerated video compositing (using NVIDIA NVENC and CUDA offloading) and partitioning media processing dynamically between Small and Big nodes, the system achieves the target scale of 1,000 concurrent rooms within a cost-effective, low-memory 40-node cluster.

The \texttt{rtp} architecture demonstrates that video conferencing signaling and media compositing can be consolidated into a share-nothing Erlang/GStreamer node. By replacing microservice networks and headless browsers with local C99 OS port interfaces and Ingress Room Sticky Sessions, the system avoids cluster synchronization locks and mesh overhead. This provides a highly resilient, cost-effective SRE model for secure networks.
\begin{thebibliography}{99}

\bibitem{rfc3264}
Rosenberg, J., and Schulzrinne, H.
\newblock An Offer/Answer Model with the Session Description Protocol (SDP).
\newblock Internet Engineering Task Force (IETF), RFC 3264, June 2002.

\bibitem{rfc3550}
Schulzrinne, H., Casner, S., Frederick, R., and Jacobson, V.
\newblock RTP: A Transport Protocol for Real-Time Applications.
\newblock Internet Engineering Task Force (IETF), RFC 3550, STD 64, July 2003.

\bibitem{rfc3711}
Baugher, M., McGrew, D., Naslund, E., Carrara, E., and Norrman, K.
\newblock The Secure Real-time Transport Protocol (SRTP).
\newblock Internet Engineering Task Force (IETF), RFC 3711, March 2004.

\bibitem{rfc8445}
Keranen, A., Mutabozi, E., and Melnikov, A.
\newblock Interactive Connectivity Establishment (ICE): A Protocol for Network Address Translator (NAT) Traversal.
\newblock Internet Engineering Task Force (IETF), RFC 8445, October 2018.

\bibitem{rfc8829}
Jennings, C., Bostrom, H., and Bruaroey, J.-I.
\newblock JavaScript Session Establishment Protocol (JSEP).
\newblock Internet Engineering Task Force (IETF), RFC 8829, January 2021.

\bibitem{w3cwebrtc}
W3C Recommendation.
\newblock WebRTC 1.0: Real-Time Communication Between Browsers.
\newblock W3C, January 2021.
\newblock Available online: \url{https://www.w3.org/TR/webrtc/}.

\bibitem{openwebrtc}
Ålund, S., and Ericsson Research.
\newblock OpenWebRTC: Transcend the Browser.
\newblock Ericsson Research Blog, September 2014.
\newblock Available online: \url{https://github.com/EricssonResearch/openwebrtc}.

\bibitem{seanslabwebrtc}
DuBois, S.
\newblock WebRTC: The Protocol That Made the Browser a Phone.
\newblock Sean's Lab Blog, April 2026.
\newblock Available online: \url{https://seanslab.org/posts/webrtc-the-protocol-that-made-the-browser-a-phone}.

\bibitem{xt601}
ITU-T Recommendation X.601.
\newblock Information technology - Multi-peer communications framework.
\newblock International Telecommunication Union, 2000.

\bibitem{xt602}
ITU-T Recommendation X.602.
\newblock Information technology - Group management protocol.
\newblock International Telecommunication Union, 2001.

\bibitem{xt609}
ITU-T Recommendation X.609.
\newblock Managed peer-to-peer communications.
\newblock International Telecommunication Union, 2010.

\bibitem{marier2020}
Marier, F.
\newblock Peer-to-peer video conferencing using GStreamer and WebRTC.
\newblock Available online: \url{https://feeding.cloud.geek.nz/posts/peer-to-peer-video-conferencing-using/}, 2020.

\bibitem{toradexgst}
Toradex Developer Center.
\newblock Video Processing with GStreamer on Linux.
\newblock Available online: \url{https://developer.toradex.com/software/linux-resources/multimedia/video-processing-gstreamer/}, 2026.

\bibitem{erpsre}
Synrc Research.
\newblock SRE Handbook: Operational Practices \& Topology.
\newblock Available online: \url{https://cd.erp.uno/sre.pdf}, 2026.

\end{thebibliography}

\end{document}
